git checkout -b feature/BAEM-001-project-setup

git branch

feature/BAEM-001-project-setup    → new feature
fix/BAEM-047-cart-total-bug       → bug fix
chore/BAEM-023-update-deps        → maintenance
hotfix/BAEM-089-payment-crash     → urgent production fix

baemont-platform/
├── public/              ← static files (images, favicon)
├── src/                 ← all your code lives here
│   ├── assets/          ← images, fonts
│   ├── components/      ← reusable UI pieces
│   ├── pages/           ← full page components
│   ├── store/           ← state management
│   ├── services/        ← API calls
│   ├── types/           ← TypeScript interfaces
│   ├── utils/           ← helper functions
│   ├── hooks/           ← custom React hooks
│   ├── styles/          ← global CSS
│   ├── App.tsx          ← root component
│   └── main.tsx         ← entry point
├── .eslintrc            ← code quality rules
├── .prettierrc          ← code formatting rules
├── index.html           ← HTML entry point
├── package.json         ← dependencies
├── tsconfig.json        ← TypeScript config
└── vite.config.ts       ← Vite config


npm create vite@latest . -- --template react-ts